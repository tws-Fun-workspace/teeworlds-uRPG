float Spreading[18*2+1];
                    for(int i = 0; i < 18*2+1; i++) {
                        Spreading[i] = -1.260f + 0.070f * i;
                    }
                    for(int i = -GetFloatAngle(); i < GetFloatAngle(); i++) {
                        float a = GetAngle(Direction);
                        a += Spreading[i+18];
                        // Direction = "   vec2(cosf(a), sinf(a))   " !!!! copy it
                        // Weapon>
                    }
